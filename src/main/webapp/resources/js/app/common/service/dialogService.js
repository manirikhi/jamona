/**
 * Post services CRUD ajax calls
 */
(function() {
    "use strict";
        app.factory('dialogService', function($cookies) {
            var dialog = {};
            
            dialog.confirm = function(header,msg,opts){
            	
            };
            return dialog;
         });
})();